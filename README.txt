Need to organize `cirq_impl` vs `qiskit_impl`:
1. `analyze_data.py` is a duplicate in both directories.
2. `google_chosen_data` is (more or less) a duplicate in both directories.
3. There are notebooks in both directories.